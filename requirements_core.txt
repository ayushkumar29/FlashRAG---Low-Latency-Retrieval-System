# First install these core dependencies
numpy>=1.26.3
pandas>=2.2.0
pydantic>=2.0.0
fastapi>=0.95.2
uvicorn[standard]>=0.27.1
python-dotenv>=1.0.0

# Then install chromadb with --no-deps
--no-deps chromadb==0.4.22

# Then install the rest of the requirements
-r requirements.txt
